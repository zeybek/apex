---
description: Diagnose a failure to a confirmed root cause, using any .apex-design/<slug>/ workspace as the intended-behavior baseline.
argument-hint: "the failure, incident, or symptom"
---

Use the apex-investigate skill to diagnose:

$ARGUMENTS

When a `.apex-design/<slug>/` planning workspace covers the affected area, use its decisions and design as the baseline for expected behavior.

Triage and mitigate active harm first, preserve evidence before it is lost, define the failure in observable terms, reproduce and bound it, read the signals end to end, test competing hypotheses cheapest discriminating test first, and confirm the root cause as a mechanism rather than a label. Hand the durable fix to `/apex-implement` with the mechanism and the reproduction.
