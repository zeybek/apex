# Development tooling. The runtime validators need none of this — they are
# standard-library only. Install with: pip install -r requirements-dev.txt
pytest>=9.1.0
pytest-cov>=7.1.0
ruff>=0.15.17
mypy>=2.1.0
