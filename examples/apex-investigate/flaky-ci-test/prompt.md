# Task

The routing test passes locally but fails intermittently in CI. Diagnose the failure before proposing a fix.

Preserve the failing behavior, use the available files and commands to bound the condition, and write `investigation.json` with these fields:

- `trigger`
- `mechanism`
- `scope`
- `mitigation`
- `durable_fix`
- `prevention`

Do not edit `routing.py` or `test_routing.py`. Separate immediate mitigation from the durable fix and explain the evidence chain in your close-out.
